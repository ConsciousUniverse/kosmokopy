[package]
name = "kosmokopy"
version = "0.1.0"
edition = "2021"
description = "GTK4 file copier/mover — recursively copies or moves files while preserving directory structure"

[dependencies]
gtk4 = { version = "0.9", features = ["v4_10"] }
walkdir = "2"
